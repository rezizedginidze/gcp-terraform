apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: balloon-priority
value: 0
globalDefault: false
description: "This priority class should be used for workloads that are balloon pods."
